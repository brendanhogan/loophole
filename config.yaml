model:
  default: "anthropic/claude-3.5-haiku"  # Or "openai/gpt-4o", "x-ai/grok-4.1-fast", "meta-llama/llama-3-70b-instruct", etc. if using OpenRouter
  max_tokens: 4096

temperatures:
  legislator: 0.4
  loophole_finder: 0.9
  overreach_finder: 0.9
  judge: 0.3

loop:
  max_rounds: 10
  cases_per_agent: 3

session_dir: "sessions"
